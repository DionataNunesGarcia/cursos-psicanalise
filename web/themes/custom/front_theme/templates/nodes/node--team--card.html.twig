<div class="group cursor-pointer">
  <div class="relative aspect-[3/4] overflow-hidden bg-neutral-100 mb-4 border-4 border-white shadow-md group-hover:border-amber-600 transition-all duration-300">
    <img 
      src="{{ file_url(content.field_team_image[0]['#media'].field_media_image.entity.uri.value)}}" 
      alt="{{ label[0]['#context']['value'] }}" 
      class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500"
    >
  </div>
  <div class="space-y-1">
    <h3 class="font-serif text-xl tracking-tight">
      {{ label[0]['#context']['value'] }}
    </h3>
    <p class="text-sm text-neutral-600">
      {# Cargo #}
      {% if content.field_team_role %}
        {% for item in content.field_team_role['#items'] %}
          <span>{{ item.entity.label }}</span>
          {% if not loop.last %}
            <span>|</span>
          {% endif %}
        {% endfor %}
      {% endif %}
    </p>
    <a href="{{ url }}" class="text-primary font-semibold hover:underline">
      Veja mais →
    </a>
  </div>
</div>


    {# Bio #}
    {# {% if content.field_bio %}
      <div class="text-gray-600 text-sm italic mb-6 line-clamp-3 mt-6 ">
        {{ content.field_bio }}
      </div>
    {% endif %} #}